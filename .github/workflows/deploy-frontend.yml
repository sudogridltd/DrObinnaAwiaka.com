# ─────────────────────────────────────────────────────────────────────────────
# Deploy Frontend (Vike SSR) → cPanel shared hosting
#
# Required GitHub Secrets (SSH only):
#   CPANEL_HOST          – server hostname or IP
#   CPANEL_PORT          – SSH port (usually 22)
#   CPANEL_USERNAME      – cPanel account username
#   CPANEL_SSH_KEY       – SSH private key (no passphrase)
#   FRONTEND_DEPLOY_PATH – absolute path on server
#   BACKEND_DEPLOY_PATH  – needed to read VITE_* vars from backend app env
#
# All app env vars (VITE_STRAPI_URL, VITE_STRAPI_API_TOKEN, NODE_ENV etc.)
# are read from cPanel Node.js Selector at build time — not stored in GitHub.
# ─────────────────────────────────────────────────────────────────────────────

name: Deploy Frontend

on:
  push:
    branches: [production]
    paths:
      - 'website/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Fetch env → Build → Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Load SSH key into agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.CPANEL_SSH_KEY }}

      - name: Add server to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.CPANEL_PORT }} ${{ secrets.CPANEL_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true

      # SSH to server, source the cPanel Node.js venv, and export the env vars
      # we need for the build. They're written into GITHUB_ENV so subsequent
      # steps can use them — nothing is stored as a GitHub Secret.
      - name: Fetch build env vars from server
        run: |
          DEPLOY_ABS="${{ secrets.FRONTEND_DEPLOY_PATH }}"
          DEPLOY_REL=$(echo "$DEPLOY_ABS" | sed 's|^/home/[^/]*/||')
          ACTIVATE="/home/${{ secrets.CPANEL_USERNAME }}/nodevenv/$DEPLOY_REL/22/bin/activate"
          # Use bash -s to run the script on the remote server.
          # cPanel stores app env vars in .env at the app root (set via the
          # Node.js Selector UI). Source it so the vars appear in printenv.
          # Also check .htaccess for PassengerEnvVar lines as a fallback.
          ssh -p ${{ secrets.CPANEL_PORT }} \
            -o StrictHostKeyChecking=no \
            ${{ secrets.CPANEL_USERNAME }}@${{ secrets.CPANEL_HOST }} \
            'bash -s' << ENDSSH
          set -a
          DEPLOY_ABS="$DEPLOY_ABS"
          ACTIVATE="$ACTIVATE"
          [ -f "\$ACTIVATE" ] && source "\$ACTIVATE"
          [ -f "\$DEPLOY_ABS/.env" ] && source "\$DEPLOY_ABS/.env"
          # Fallback: parse PassengerEnvVar directives from .htaccess
          if [ -f "\$DEPLOY_ABS/.htaccess" ]; then
            while IFS= read -r line; do
              if [[ "\$line" =~ ^PassengerEnvVar[[:space:]]+([A-Za-z_][A-Za-z0-9_]*)[[:space:]]+(.*)$ ]]; then
                export "\${BASH_REMATCH[1]}"="\${BASH_REMATCH[2]}"
              fi
            done < "\$DEPLOY_ABS/.htaccess"
          fi
          set +a
          # Log what was found (values masked) for debugging
          echo "--- env vars captured ---" >&2
          printenv | grep -E '^(VITE_|NODE_ENV)=' | sed 's/=.*/=***/' >&2
          # Output the actual values to GITHUB_ENV
          printenv | grep -E '^(VITE_|NODE_ENV)=' || true
          ENDSSH

      - name: Verify build env vars were captured
        run: |
          echo "VITE_STRAPI_URL present: ${{ env.VITE_STRAPI_URL != '' }}"
          echo "VITE_STRAPI_API_TOKEN present: ${{ env.VITE_STRAPI_API_TOKEN != '' }}"
          echo "NODE_ENV: ${{ env.NODE_ENV }}"
          if [ -z "$VITE_STRAPI_URL" ]; then
            echo "⚠️  WARNING: VITE_STRAPI_URL is empty — build will use localhost:1337 fallback"
          fi

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Get pnpm store directory
        id: pnpm-cache
        run: echo "store=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.store }}
          key: pnpm-store-${{ hashFiles('website/pnpm-lock.yaml') }}
          restore-keys: pnpm-store-

      - name: Install dependencies
        working-directory: website
        run: pnpm install --no-frozen-lockfile

      - name: Build Vike app + compile server
        working-directory: website
        run: pnpm build:all
        # VITE_* and NODE_ENV are already in the environment from the fetch step

      - name: Sync build to server
        run: |
          rsync -az --delete \
            -e "ssh -p ${{ secrets.CPANEL_PORT }} -o StrictHostKeyChecking=no" \
            --exclude='.env' \
            --exclude='.env.*' \
            --exclude='node_modules' \
            --filter='protect node_modules' \
            --exclude='.git' \
            --exclude='tmp' \
            --include='dist/' \
            --include='dist/**' \
            --include='public/' \
            --include='public/**' \
            --include='server.js' \
            --include='package.json' \
            --include='vite.config.production.js' \
            --exclude='*' \
            website/ ${{ secrets.CPANEL_USERNAME }}@${{ secrets.CPANEL_HOST }}:${{ secrets.FRONTEND_DEPLOY_PATH }}/

      - name: Install production deps & restart
        run: |
          ssh -p ${{ secrets.CPANEL_PORT }} \
            -o StrictHostKeyChecking=no \
            ${{ secrets.CPANEL_USERNAME }}@${{ secrets.CPANEL_HOST }} bash << 'EOF'
            set -e
            DEPLOY_ABS="${{ secrets.FRONTEND_DEPLOY_PATH }}"
            DEPLOY_REL=$(echo "$DEPLOY_ABS" | sed 's|^/home/[^/]*/||')
            # Remove real node_modules if present — CloudLinux requires it to be a symlink
            if [ -d "$DEPLOY_ABS/node_modules" ] && [ ! -L "$DEPLOY_ABS/node_modules" ]; then
              rm -rf "$DEPLOY_ABS/node_modules"
            fi
            # Source cPanel Node.js venv so npm is on PATH
            source "/home/${{ secrets.CPANEL_USERNAME }}/nodevenv/$DEPLOY_REL/22/bin/activate"
            cd "$DEPLOY_ABS"
            npm install --omit=dev --ignore-scripts
            # Use the minimal production vite config so Vike doesn't load the
            # full vite.config.ts (which imports devDeps not present in prod).
            # vite.config.js is resolved before vite.config.ts by Vite.
            cp vite.config.production.js vite.config.js
            mkdir -p tmp && touch tmp/restart.txt
            echo "✅ Frontend deployed successfully"
          EOF
